import React from "react";
import './Error.scss'

const Error = () => {
    return (
        <div className="Error-area">
            Not Allowed to access;
        </div>
    )
};

export default Error;